{
  "agent_name": "performance_brain",
  "output_key": "performance_strategy",
  "description": "Hardened 2.0 Lean Pivot: Unified agent that merges growth analytics with performance optimization rules.",
  "schema": {
    "measurement_philosophy": "string",
    "kpis": ["string"],
    "north_star_metric": "string",
    "optimization_principles": "string",
    "pause_rules": ["string"],
    "scale_rules": ["string"],
    "budget_shifts": ["string"],
    "directives": [
      {
        "action": "string",
        "description": "string",
        "risk_score": "number",
        "confidence": "number",
        "amount_value": "number",
        "reasoning": "string"
      }
    ],
    "learned_anti_patterns": ["string"],
    "next_step": "string | null",
    "refinement_context": "string | null"
  }
}
